name: my_music_app
description: A Flutter application to browse and discover music from Spotify and YouTube.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2  # Library untuk HTTP request
  provider: ^6.0.0  # Library untuk state management
  cached_network_image: ^3.2.1  # Library untuk caching gambar
  flutter_svg: ^1.0.3  # Library untuk SVG support (opsional)

flutter:
  uses-material-design: true
  assets:
    - lib/assets/images/loading.png
    - lib/assets/images/music_cross.png

  # fonts:
  #   - family: Roboto
  #     fonts:
  #     - asset: assets/fonts/Roboto-Regular.ttf
  #     - asset: assets/fonts/Roboto-Bold.ttf
  #       weight: 700
  #     #   - asset: assets/fonts/CustomFont-Regular.ttf
dev_dependencies:
  flutter_lints: ^2.0.0  # Versi bisa disesuaikan
  flutter_test:
    sdk: flutter
